import { Routes } from '@angular/router';

// NOTE: Routes are defined in app-routing.module.ts using AppRoutingModule
// Do NOT add routes here to avoid dual routing configuration conflicts
// that cause NG04016 infinite redirect errors

export const routes: Routes = [];
